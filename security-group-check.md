# 보안 그룹 확인 및 설정 가이드

## 🔍 Private-SG 규칙 확인

### **AWS 콘솔에서 확인:**
```
1. EC2 콘솔 → 보안 그룹
2. Private-SG 선택
3. 인바운드/아웃바운드 규칙 확인
```

### **Worker에 필요한 최소 규칙:**
```
인바운드:
- SSH (22): 관리자 IP 또는 0.0.0.0/0
- HTTP (3000): 헬스체크용 (선택사항)

아웃바운드:
- ClickHouse (9000): Write-Only 서버 IP
- HTTPS (443): npm, git 등
- HTTP (80): apt 등
```

## 🎯 권장사항

### **Private-SG가 이미 적절하다면:**
```
✅ 기존 Private-SG 사용
- 추가 설정 불필요
- 관리 편의성
```

### **Private-SG가 너무 광범위하다면:**
```
🔄 Worker 전용 보안 그룹 생성
- 최소 권한 원칙
- 보안 강화
```

## 📋 확인 체크리스트

### **Private-SG에 다음 규칙이 있는지 확인:**
```
✅ SSH (22) 인바운드 허용
✅ 아웃바운드 HTTPS (443) 허용
✅ 아웃바운드 HTTP (80) 허용
```

### **Write-Only 서버 연결 확인:**
```
✅ ClickHouse (9000) 포트로 Write-Only 서버 접근 가능
✅ 같은 VPC 내에서 통신 가능
```

## 🚀 최종 추천

### **현재 상황에서:**
```
Private-SG가 적절한 규칙을 가지고 있다면 → 기존 사용
Private-SG가 너무 광범위하다면 → Worker 전용 생성
```

### **빠른 시작을 원한다면:**
```
✅ 기존 Private-SG 사용 (현재 설정 유지)
- 추가 설정 불필요
- 바로 시작 가능
```

### **보안을 중시한다면:**
```
🔄 Worker 전용 보안 그룹 생성
- 최소 권한 원칙 적용
- 보안 강화
```

## 💡 실용적 접근

### **1단계: 기존 Private-SG로 시작**
```
- 빠르게 시작
- 문제 없으면 그대로 사용
```

### **2단계: 필요시 보안 그룹 분리**
```
- 보안 정책 강화 필요시
- Worker 전용 보안 그룹 생성
``` 